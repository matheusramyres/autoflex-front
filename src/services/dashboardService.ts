import sumary from '../data/summary.json';

export const fetchDashboardSummary = async () => {
  // const response = await api.get('/dashboard/summary');
  // return response.data;
  return sumary;
};
